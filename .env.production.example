# =============================================================================
# MosqOS Production Environment Variables
# =============================================================================
# Copy this file to .env.production and fill in your production values
# IMPORTANT: Never commit .env.production to git!
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Your Supabase project URL (found in Supabase Dashboard > Settings > API)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anon/public key (safe to expose in client-side code)
# Found in: Supabase Dashboard > Settings > API > Project API keys > anon public
VITE_SUPABASE_ANON_KEY=your-production-anon-key

# ⚠️  CRITICAL: DO NOT SET SERVICE_ROLE_KEY IN PRODUCTION!
# Service role key bypasses Row Level Security (RLS) and should NEVER be exposed
# to client-side code. Only use in development mode for seeding/testing.
# VITE_SUPABASE_SERVICE_ROLE_KEY=DO-NOT-SET-IN-PRODUCTION

# -----------------------------------------------------------------------------
# Stripe Configuration
# -----------------------------------------------------------------------------
# Stripe publishable key for production (starts with pk_live_...)
# Found in: Stripe Dashboard > Developers > API keys > Publishable key
# ⚠️  Use TEST keys (pk_test_...) for staging environments!
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your-production-key

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Your production domain (no trailing slash)
VITE_APP_URL=https://your-domain.com

# Application name (displayed in UI, emails, etc.)
VITE_APP_NAME=MosqOS

# -----------------------------------------------------------------------------
# Optional: Analytics & Monitoring
# -----------------------------------------------------------------------------
# Add analytics/monitoring keys here if needed
# VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# -----------------------------------------------------------------------------
# Build Configuration
# -----------------------------------------------------------------------------
# Node environment (should be 'production' for production builds)
NODE_ENV=production

# =============================================================================
# SECURITY CHECKLIST BEFORE DEPLOYING:
# =============================================================================
# [ ] Using PRODUCTION Supabase URL and anon key (not development project)
# [ ] Using LIVE Stripe keys (pk_live_..., NOT pk_test_...)
# [ ] SERVICE_ROLE_KEY is NOT set or exposed in client code
# [ ] VITE_APP_URL matches your production domain
# [ ] SSL/HTTPS is enabled on your domain
# [ ] Supabase RLS policies are enabled on all tables
# [ ] Database backups are configured in Supabase
# [ ] Stripe webhook endpoints are configured for production
# [ ] Environment variables are set in hosting provider (Vercel/Netlify)
# =============================================================================
